// config.js

var config = {

    // debug 为 true 时，用于本地调试
    debug: true,
    port: 4000,
    hostname: 'localhost',
    jiraHost: 'jira.atlassian.com',

    // title
    title: '网仓Jira报表系统'

};

module.exports = config;